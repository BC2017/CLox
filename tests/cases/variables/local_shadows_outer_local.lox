// A nested block can declare a local that shadows an outer block's local.
// After the inner block ends, the outer local is visible again.
// expect: 1
// expect: 2
// expect: 1
{
  var x = 1;
  print x;
  {
    var x = 2;
    print x;
  }
  print x;
}
